// ignore_for_file: file_names

import 'package:flutter/material.dart';

class SakuraHome extends StatelessWidget {
  const SakuraHome({ Key? key }) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Container(
      
    );
  }
}